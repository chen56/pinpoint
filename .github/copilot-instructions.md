# Copilot / AI Coding Agent Instructions

- Use `.ai/dependency-context.json` as the first source for dependency/version assumptions.
- If dependencies change, run `python scripts/export_dependency_context.py`.
- Prefer minimal diffs and explicit tests/checks.
- Follow Conventional Commits for commit messages.
